# Plan 000014 | REFACTOR remove win-rate eval harness | 2026-06-20 19:06 UTC | remove win-rate eval harness | Review: light
plan_format_version: 1

## User Brief

will not use this win-rate eval strategy. remove it from code

## Agent Interpretation

**Problem**: The win-rate evaluation harness (`run_win_rate`, `summarize_win_rate`, `_resolve_checkpoint` in `eval.py`, plus its test file and docs) was introduced in plan-000008 but the researcher has decided not to use this evaluation strategy. The dead code should be removed cleanly.

**Approach**: Delete the three functions that implement the harness, the four CLI args that drive it, the dispatch block in `main()`, the dedicated test file, and the corresponding README sentence and as-coded description. The `classify_outcome` helper is shared with the render path (`_build_final_result`) and must be kept.

## Best Practices

- Touch only dead code; leave `classify_outcome` and the `csv` import (still used by line 72's reader) in place.
- Delete the test file rather than gutting it — an empty test file would be misleading.
- Keep plan-000008 plan artifacts as historical record; do not delete them.

## Steps

### Step 1 — Remove `summarize_win_rate`, `_resolve_checkpoint`, and `run_win_rate` from `eval.py`

- [ ] Delete `def summarize_win_rate(...)` (lines 215–240) 
- [ ] Delete `def _resolve_checkpoint(...)` (lines 267–279)
- [ ] Delete `def run_win_rate(...)` (lines 281–378)

**Files**: `custom_environment/eval.py`
**References**: N/A
**Interface**: N/A
**Verify**: `grep -n "summarize_win_rate\|run_win_rate\|def _resolve_checkpoint" custom_environment/eval.py` returns nothing (excluding the `_resolve_checkpoint_view_size` match)
**Tests**: N/A
**Depends on**: —

---

### Step 2 — Remove win-rate CLI args and dispatch block from `main()` in `eval.py`

- [ ] Remove `--seed` argument block (lines 704–709 — "Base seed for win-rate episodes")
- [ ] Remove `--episodes` argument block (lines 710–715)
- [ ] Remove `--eval-epsilon` argument block (lines 716–725)
- [ ] Remove `--win-rate-out` argument block (lines 726–731)
- [ ] Remove the dispatch block (lines 821–835: `if args.episodes > 1 or args.win_rate_out is not None: ... return`)

**Files**: `custom_environment/eval.py`
**References**: N/A
**Interface**: N/A
**Verify**: `grep -n "episodes\|eval_epsilon\|win_rate_out\|run_win_rate" custom_environment/eval.py` returns nothing
**Tests**: N/A
**Depends on**: Step 1

---

### Step 3 — Delete `test/test_eval_win_rate.py`

- [ ] Delete the file `test/test_eval_win_rate.py`

**Files**: `test/test_eval_win_rate.py`
**References**: N/A
**Interface**: N/A
**Verify**: `py -3.11 -m pytest test/ -q` passes (remaining tests still green)
**Tests**: N/A
**Depends on**: Steps 1–2

---

### Step 4 — Update README.md

- [ ] Line 62: remove the sentence `with the [Win-Rate Evaluation](#win-rate-evaluation-does-iql-actually-win) harness.` (or reword so it no longer references the harness link)
- [ ] Line 136: remove `--episodes 50 --eval-epsilon 0.05 --win-rate-out path\to\iql_win_rate.csv --seed 0` from the eval optional-parameters block

**Files**: `README.md`
**References**: N/A
**Interface**: N/A
**Verify**: `grep -n "win.rate\|eval.epsilon\|win_rate_out\|--episodes\|--seed\b" README.md` shows only `eval_report.py`-related matches (lines 169–170 are `eval_report.py` output fields — fine to keep)
**Tests**: N/A
**Depends on**: —

---

### Step 5 — Update `product-design/project/product-design-as-coded.md`

- [ ] In §3 Domain-Specific Concepts, remove the "Win-rate evaluation harness" bullet (the entire paragraph starting "Win-rate evaluation harness: `custom_environment/eval.py` exposes a headless multi-episode mode...") 

**Files**: `product-design/project/product-design-as-coded.md`
**References**: N/A
**Interface**: N/A
**Verify**: `grep -n "win.rate\|win_rate\|eval_epsilon\|run_win_rate" product-design/project/product-design-as-coded.md` returns nothing
**Tests**: N/A
**Depends on**: —

---

## Outcomes

- `eval.py` contains no win-rate harness code; `--episodes`, `--eval-epsilon`, `--win-rate-out`, and `--seed` args are gone.
- `test/test_eval_win_rate.py` is deleted; remaining test suite passes.
- README and as-coded doc no longer reference the win-rate strategy.
- `classify_outcome` and the `csv` import remain (they serve the render path).

## Smoke

false
